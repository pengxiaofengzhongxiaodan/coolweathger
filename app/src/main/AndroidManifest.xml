<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pxfandroid.coolweather">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pxfandroid.coolweather.acitvity.ChooseAreaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pxfandroid.coolweather.acitvity.WeatherActivity"></activity>
        <service android:name="pxfandroid.coolweather.service.AutoUpdateService"></service>
        <receiver android:name="pxfandroid.coolweather.receiver.AutoUpdateReceiver"></receiver>
    </application>

</manifest>